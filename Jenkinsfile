pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build action is ${action}'
            }
        }
         stage('Test') {
            steps {
                echo 'Test is ${action}'
            }
        }
    }
}
